name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  archived:
    name: Archived
    runs-on: ubuntu-latest
    steps:
      - run: echo "This crate has been archived and merged into the apiari monorepo."
